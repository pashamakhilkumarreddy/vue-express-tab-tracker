const {
  songs
} = require('./songs');
const {
  users
} = require('./users');
const {
  bookmarks
} = require('./bookmarks');

const {
  histories
} = require('./histories');

module.exports = {
  songs,
  users,
  bookmarks,
  histories,
}